# Ozon Dashboard — правила для агента

## Пуш на GitHub
После любых изменений в этом проекте **сразу пушить на GitHub**: `git add -A && git commit -m "..." && git push origin main`. Не предлагать пользователю «теперь выполни push» — делать коммит и пуш самому.
